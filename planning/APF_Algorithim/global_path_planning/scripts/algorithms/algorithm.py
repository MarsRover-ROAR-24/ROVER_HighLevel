# from .astar import astar
from .APF import apf_path_planning
# from .APF import waypoints



def apf_path_planning(start, goal, obstacles, step_size=0.1, max_iters=1000):
    return apf_path_planning(start, goal, obstacles, step_size=0.1, max_iters=1000)
# def algorithm(start_index, goal_index, width, height, costmap, resolution, origin, viz):
#     return astar(start_index, goal_index, width, height, costmap, resolution, origin, viz)

# def algorithm(start_index, goal_index, width, height, costmap, resolution, origin, viz):
#     return waypoints

